// interfaces describes the properties of an object
// Uses so any features needing the data that has the same description

export interface PetInfo {
    name : string,
    type : string,
    gender : string
}
